Input (784)
    ↓
Linear(784→512) → BatchNorm → ReLU → Dropout(0.3)
    ↓
Linear(512→256) → BatchNorm → ReLU → Dropout(0.3)
    ↓
Linear(256→128) → BatchNorm → ReLU → Dropout(0.3)
    ↓
Linear(128→10)  ← raw logits → CrossEntropyLoss